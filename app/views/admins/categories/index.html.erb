<h1 class="page-title">ジャンル一覧・追加</h1>
<div class="row">
  <div class="col-xs-6">
    <div class="category-edit-form">
      <%= form_for @category, url: admins_categories_path do |f| %>
        <%= f.label :name, "ジャンル" %>
        <%= f.text_field :name %>
        <label><%= f.radio_button :is_active, true, checked: true %> 有効 </label>
        <label><%= f.radio_button :is_active, false %> 無効 </label>
        <%= f.submit "追加", class: "btn btn-primary btn-md" %>
      <% end %>
    </div>
  </div>
</div>
<div class="row">
  <div class="col-xs-6 margin-30">
    <table class="table">
      <thead>
        <tr class="active">
          <th style="width:30%">ジャンル</th>
          <th style="width:40%">状態</th>
          <th style="width:30%"></th>
        </tr>
      </thead>
      <tbody>
        <% @categories.each do |category| %>
          <tr>
            <td><%= category.name %></td>
            <td>
              <% if category.is_active? %>
                有効
              <% else %>
                無効
              <% end %>
            </td>
            <td>
              <%= link_to "編集する", edit_admins_category_path(category), class: "btn btn-primary btn-md" %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>