<div class="mypage-title">
  <mark style="font-size: 20px;">商品詳細</mark>
</div>

<div class="row">
  <div class="col-xs-5 coa-xs-offset-1">
    <div class="image-box">
      <% if @product.image_id.blank? %>
        <%= image_tag "no-image.jpg", alt:"写真", width:"350px", height:"250px" %>
      <% else %>
        <%= attachment_image_tag @product, :image, :fill, 350, 250, format: "jpg" %>
      <% end %>
    </div>
  </div>
  <div class="col-xs-5">
    <div class="table margin-30">
      <table border="0">
      <tr>
        <th style="width:200px">商品名</th>
        <td style="font-size:20px"><%= @product.name %></td>
      </tr>
      <tr>
        <th>商品説明</th>
        <td><%= @product.introduction %></td>
      </tr>
      <tr>
        <th>ジャンル</th>
        <td style="font-size:20px"><%= @product.category.name %></td>
      </tr>
      <tr>
        <th>税込価格(税抜き価格)</th>
        <td style="font-size:20px">¥ <%= tax_included_price(@product.price).to_i %>　(<%= @product.price %>）</td>
      </tr>
      <tr>
        <th>販売ステータス</th>
        <td style="font-size:20px">
          <% if @product.is_saling? %>
            販売中
          <% else %>
            販売停止中
          <% end %>
        </td>
      </tr>
      </table>
    </div>
  </div>
</div>
<div class="row">
  <div class="col-xs-2 col-xs-offset-10">
    <%= link_to "商品編集", edit_admins_product_path(@product), class: "btn btn-primary btn-lg" %>
  </div>
</div> 