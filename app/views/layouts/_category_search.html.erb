<div class="category border">
  <div class="c-search">
    <h4>ジャンル検索</h4>
  </div>
  <div class="c-name">  
    <p><%= link_to "全ジャンル", products_path %></p>
    <% @categories.each do |category| %>
      <p><%= link_to category.name, products_path(category_id:category.id) %></p>
    <% end %>
  </div>
</div>