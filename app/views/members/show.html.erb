<div class="mypage-title">
  <mark style="font-size: 20px;">マイページ</mark>
</div>

<div class="row">
  <div class="col-xs-7">
    <div>
      <h1 class="main-title">登録情報</h1>
      <%=  link_to "編集する", edit_member_path, class:"btn btn-primary" %>
      <%=  link_to "パスワードを変更する", edit_member_registration_path, class:"btn btn-primary",style:"margin-left:10px;" %>
    </div>
    <table class="table table-bordered">
      <tr>
        <th>氏名</th>
        <td>
          <%= "#{current_member.last_name}　#{current_member.first_name}" %>
        </td>
      </tr>

      <tr>
        <th>カナ</th>
        <td>
          <%= "#{current_member.kana_last_name}　#{current_member.kana_first_name}" %>
        </td>
      </tr>

      <tr>
        <th>郵便番号</th>
        <td>
          <%= current_member.postal_code %>
        </td>
      </tr>

      <tr>
        <th>住所</th>
        <td>
          <%= current_member.address %>
        </td>
      </tr>

      <tr>
        <th>電話番号</th>
        <td>
          <%= current_member.phone_number %>
        </td>
      </tr>

      <tr>
        <th>メールアドレス</th>
        <td>
          <%= current_member.email %>
        </td>
      </tr>

    </table>
    <div>
      <h1 class="main-title" style="margin-right:26px;">配送先</h1>
      <%=  link_to "一覧を見る", member_addresses_path(@member), class:"btn btn-primary"%>
    </div>
    <div>
      <h1 class="main-title">注文履歴</h1>
      <%=  link_to "一覧を見る", member_orders_path(@member), class:"btn btn-primary" %>
    </div>
  </div>
</div>