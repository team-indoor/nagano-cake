<h2>パスワードをお忘れになられた方へ</h2><br>
<p>
  以下の欄に登録メールアドレスを入力してください。<br>
  パスワード再設定に関して、メールにてご連絡差し上げます。
</p>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field", style="margin-top:30px">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div style="margin:25px 0 50px 0">
    <%= f.submit "パスワードをリセットする", class:"btn btn-primary" %>
  </div>
<% end %>

<%= link_to "ログインする", new_member_session_path, style:"margin-right:10px" %>
<%= link_to "新規登録する", new_member_registration_path %>
