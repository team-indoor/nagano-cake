<h3>配送先編集</h3>

<%= form_for @address, :url => {:action => :edit} do |f| %>

	<h4>郵便番号（ハイフンなし）</h4>
	<%= f.text_field :postal_code %>

	<h4>住所</h4>
	<%= f.text_area :address %>

	<h4>宛名</h4>
	<%= f.text_field :reciever %>

	<%= f.submit("編集する",class: 'btn btn-primary') %>

<% end %>